/// Central app-wide constants.
///
/// Update [appVersion] here whenever the version in pubspec.yaml changes
/// to keep the two in sync.
class AppConstants {
  AppConstants._();

  /// Matches the `version` field in pubspec.yaml (without the build number).
  static const String appVersion = '1.0.0';
}
